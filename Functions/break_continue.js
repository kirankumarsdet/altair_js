var array=[1,'kiran',123, 'tyss']
for (let index = 0; index < array.length; index++) 
{
    if (array<5) 
    {
        console.log(index+"   "+array[index])
        continue
    }
   console.log('kiranKumar')
    
}